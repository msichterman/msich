{
  "scripts": {
    "setup": "pnpm install && vercel env pull .env.local --yes 2>/dev/null || echo 'Vercel env pull skipped, ensure .env.local exists'",
    "run": "pnpm dev -- -p ${CONDUCTOR_PORT:-3000}"
  },
  "runScriptMode": "nonconcurrent"
}
